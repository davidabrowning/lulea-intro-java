package TaskSet04;

// Imports

/**
 * TASK:
 * 1.   Make a loop ( for ) that prints the interval of integer digits 0..9
 * 2.   Construct a loop ( for ) that prints all lowercase letters of the 
 *      English alphabet. Use the decimal code (ASCII code) found in the 
 *      Unicode table. Use type cast from integer to char to make a simple 
 *      conversion between the index of the decimal code and the resulting 
 *      character. (*)
 * 3.   Modify the above solution so vowels and consonants can be 
 *      identified during iteration. (*)
 * 
 * PROGRAM DESCRIPTION:
 * 1. Loop to print intervals 0..9
 * 2. Loop to print all lowercase letters
 * 3. Loop to print all lowercase letters, indicating vowels and consonants
 * 
 * @author David Browning (davbro-4)
 */
public class Task0410 {

    // Constants

    public static void main(String[] args) {

        // Variables

        // 1. Loop to print intervals 0..9
        System.out.println("Print numbers 0..9:");
        for (int i = 0; i < 10; i++) {
            System.out.println(i);
        }


    }
    
}
